https://stepik.org/lesson/24460/step/10?unit=6766
Реализуйте программу, которая будет эмулировать работу с пространствами имен. Необходимо реализовать поддержку создания
пространств имен и добавление в них переменных.

Формат входных данных

В первой строке дано число n (1 ≤ n ≤ 100) – число запросов.
В каждой из следующих n строк дано по одному запросу.
Запросы выполняются в порядке, в котором они даны во входных данных.
Имена пространства имен и имена переменных представляют из себя строки длины не более 10, состоящие из строчных латинских букв.
Формат выходных данных

Для каждого запроса get выведите в отдельной строке его результат.


Sample Input:

9
add global a
create foo global
add foo b
get foo a
get foo c
create bar foo
add bar a
get bar a
get bar b

Sample Output:

global
None
bar
foo

===
Убил два часа на решение этой задачи, потому что не очень хорошо знаком с Python.
Технические подсказки, что сэкономят ваши нервы и время.
﻿1.  Считать сразу три слова:

   cmd, namesp, arg = input().split()

2.  Наиболее простой способ хранить и обрабатывать данные - это словарь,
            где ключ - имя_пространства (parent),  значение - [переменные*, имена_пространств(children)]
3. У кого возникает ошибка "jailed code, KeyError ....".
           Наверняка вы пытаетесь найти что-то в list'e, которого просто не существует.
           Чтобы проследить, попробуйте пошаговую отладку вашей программы.
4. запрос get(namespace, var) легко реализуется рекурсией. Если var лежит в namespace, то return namespace, если нет, то return get(parent_of_namespace, var) и так до global'a.

